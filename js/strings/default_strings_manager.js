'use strict';

/**
 * The default version of the strings manager.
 */
class DefaultStringsManager {
  constructor() {
  }

  /**
   * Gets the string for the lives counter.
   * @return {string} the text to display on the lives counter.
   */
  getLivesCounterString() {
    return 'Lives';
  }
}
